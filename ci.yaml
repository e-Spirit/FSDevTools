team: core-platform
responsible:
  - reinartz
  - kafmann
artifacts:
  - name: CLI Artifacts
    location: "build/distributions"
    pattern: "*.*"
docker:
  images:
    - docker.artifactory.e-spirit.de/cxt/base:jdk-lts
