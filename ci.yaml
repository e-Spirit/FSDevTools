team: core-platform
responsible:
  - reinartz
  - kafmann
artifacts:
  - name: CLI Artifacts
    location: "build/distributions"
    pattern: "*.tar.gz"
docker:
  images:
    - docker.artifactory.e-spirit.de/cxt/base:jdk-old-lts
