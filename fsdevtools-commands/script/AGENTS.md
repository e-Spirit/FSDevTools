# fsdevtools-commands:script — Script Commands

CLI commands for parsing and executing scripts against a FirstSpirit server: `parse`, `run`. Supports BeanShell (built-in), Groovy, and JavaScript via engine plugins.

## Reference Files

| File | When to read |
|---|---|
| `AGENTS-COMMANDS.md` | CLI usage syntax, subcommands, script engine table, and key classes |
| `AGENTS-BUILD.md` | Dependencies and build commands |
