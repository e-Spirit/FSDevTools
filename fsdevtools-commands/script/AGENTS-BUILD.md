# Build & Dependencies

## Dependencies

```kotlin
implementation(project(":fsdevtools-cli-api"))
implementation(project(":fsdevtools-common"))
```

## Build

```bash
./gradlew :fsdevtools-commands:script:build
./gradlew :fsdevtools-commands:script:test
```
