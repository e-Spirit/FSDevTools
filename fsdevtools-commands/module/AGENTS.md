# fsdevtools-commands:module — Module Commands

CLI commands for managing FirstSpirit modules (FSM files): `install`, `install-bulk`, `configure`.

## Reference Files

| File | When to read |
|---|---|
| `AGENTS-COMMANDS.md` | CLI usage syntax, available subcommands, and key classes |
| `AGENTS-BUILD.md` | Dependencies and build commands |
