# fsdevtools-commands:server — Server Commands

CLI commands for starting and stopping a local FirstSpirit server process: `start`, `stop`. Delegates lifecycle logic to `fsdevtools-serverrunner`.

## Reference Files

| File | When to read |
|---|---|
| `AGENTS-COMMANDS.md` | CLI usage syntax, available subcommands, and key classes |
| `AGENTS-BUILD.md` | Dependencies and build commands |
