# Build & Dependencies

## Dependencies

```kotlin
implementation(project(":fsdevtools-cli-api"))
implementation(project(":fsdevtools-common"))
```

## Build

```bash
./gradlew :fsdevtools-commands:server:build
./gradlew :fsdevtools-commands:server:test
```
