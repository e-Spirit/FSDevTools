# Build & Dependencies

## Dependencies

```kotlin
implementation(project(":fsdevtools-cli-api"))
implementation(project(":fsdevtools-common"))
```

## Build

```bash
./gradlew :fsdevtools-commands:schedule:build
./gradlew :fsdevtools-commands:schedule:test
```
