# fsdevtools-commands:test — Test Commands

CLI commands for verifying server and project accessibility: `connection`, `project`. Useful in CI pipelines as a readiness gate before running other commands.

## Reference Files

| File | When to read |
|---|---|
| `AGENTS-COMMANDS.md` | CLI usage syntax, available subcommands, key classes, and CI usage pattern |
| `AGENTS-BUILD.md` | Dependencies and build commands |
