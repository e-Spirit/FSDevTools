# fsdevtools-cli — CLI Application Entry Point

Main application module: wires all command groups, handles CLI argument parsing, manages the FirstSpirit connection lifecycle, and produces the uber JAR.

## Reference Files

| File | When to read |
|---|---|
| `AGENTS-OVERVIEW.md` | Understanding key classes, command discovery, execution flow, or the result file mechanism |
| `AGENTS-BUILD.md` | Running or debugging the build, or diagnosing test failures (JVM flag issues) |
